# Default model (used by app and tests if --model not provided)
MODEL=Bielik-4.5B-v3.0-Instruct.Q8_0.gguf

# OpenAI (cloud)
OPENAI_API_KEY=...

# Local (OpenAI-compatible) endpoint defaults
# By default we point to LMS Studio’s OpenAI-compatible server
# You can override these in your environment if needed.
LOCAL_BASE_URL=http://localhost:1234/v1
LOCAL_API_KEY=EMPTY

# Langfuse (optional)
LANGFUSE_SECRET_KEY=sk-..
LANGFUSE_PUBLIC_KEY=pk-..
LANGFUSE_BASE_URL=http://localhost:9300
